---
title: "TaskBoard"
summary: "방향성 비순환 그래프(DAG, Directed Acyclic Graph) 구조를 채택하여 프로젝트의 선후 의존관계와 병목 구간을 동적으로 추적하는 3-Tier 기반의 유기적 업무 현황 공유 시스템입니다."
date: "2026"
category: "lab"
---

## 프로젝트의 핵심 컨셉 및 개발 목적

LIVE 네트워킹을 응용할 것을 예상하고 프레임워크를 구현한 1차 태스크보드 솔로 버전이 개발되었고, LIVE 엔진의 완성에 맞춰 로그인 로직과 서버를 구현한 뒤 서버 간 통신에 엔진 전체, 서버-클라이언트 간 통신에 데이터 프레이밍, 패킷 직렬화/파싱, 디스패치 및 스레드 동기화 모듈을 활용하여 구현된, 베타 테스트 중인 업무 현황 공유 시스템입니다. 실질적으로 LIVE 엔진이 개발된 최초의 원인을 제공한 프로젝트이기도 합니다.

## 핵심 아키텍처 및 메인 설계 방향

방향성 비순환 그래프(DAG, Directed Acyclic Graph) 구조를 채택하여 프로젝트의 선후 의존관계와 병목 구간을 동적으로 추적하는 3-Tier 기반의 유기적 업무 현황 공유 시스템입니다. Jira 등의 프로젝트에서 활용하는 전통적인 칸반 보드나 Gantt 차트의 한계를 넘어, 기획 변경에 대한 가소성을 확보하면서도 물리적 에어갭(Air-Gap) 및 양자내성 암호(PQC) 기반의 강력한 보안성을 제공하는 메인 게임플레이/작업 루프 스타일을 채택했습니다.

## 적용된 개발 방법론

1주 단위 애자일 스프린트 및 이중 소통 채널 기반 협업 프로세스 (1-Week Agile Sprint & Dual-Channel Collaboration)

프로젝트를 1주 단위의 짧은 애자일 스프린트(Agile Sprint)로 나누어 진행했습니다. 정기 스프린트 회의를 통해 지난 주차의 상세 작업 내역을 리뷰하고, 프로젝트 완성을 위해 필요한 기능적 요구사항과 백로그를 정밀하게 산정해 다음 주차 일정에 할당했습니다.

또한 개발 진행 중 이슈나 미처 예상치 못한 문제로 인해 불필요한 시간이 소모되는 것을 방지하기 위해 디스코드(Discord)와 카카오톡(KakaoTalk) 2개 이상의 이중 커뮤니케이션 채널을 상시 가동하여 실시간으로 이상 유무를 공유하고 즉각적으로 피드백을 주고받는 협업 프로세스를 유지했습니다.

## 구현에 필요했던 주요 시스템 목록

이를 위해 다음 시스템들을 만들었습니다:
1. 노드 상태 자동 전파(State Cascade Propagation) 및 실시간 병목 파악 시스템
2. DFS 기반 순환 참조 방지(Cycle Prevention) 알고리즘
3. 앱 서버와 DB 서버 간 물리적 에어갭(Air-Gap) 이중 격리 네트워크 아키텍처
4. Whitelist 및 Google OAuth 기반 이중 인증/접근 통제 시스템
5. LIVE DTS 전송 엔진 기반의 양자내성 암호(PQC / ML-KEM) 패킷 통신 프레임워크
6. 사용자 액션 추적을 위한 작업 감사 로그(NodeEditLog) 시스템
7. Read-Only 앱 서버 모니터 및 물리적 접근 전용 DB 마스터 대시보드 관제 체계

## 핵심 시스템의 세부 구현 방식

노드 상태 시스템은 선행 노드들의 완료 여부에 따라 PENDING, AVAILABLE, FINISHED 상태를 자동으로 재계산하여 전파합니다. 특정 업무가 지연되면 하위 작업들이 PENDING 상태로 묶여 시각적으로 즉각 병목 지점이 드러나게 만들었습니다. 또한, 기획 변경 시 발생할 수 있는 잘못된 순환 의존성(Loop) 생성을 차단하기 위해 클라이언트와 서버 양쪽에서 DFS 기반 검증 알고리즘을 수행하도록 구축했습니다.

보안 및 네트워크 아키텍처 측면에서는 두 서버를 라즈베리 파이 5 두 대에 각각 올렸으며, DB 서버의 외부 인터넷망을 단절하고, 앱 서버와 1:1 비관리형 스위치 전용 라인(LIVE 바이너리 TCP 패킷만 허용)으로만 연결하여 물리적 에어갭을 완성했습니다. 특히 마스터 관리자 대시보드(Port 8080)는 원격 웹 프록시 경로를 일체 차단하여 오직 물리적 실물 서버 콘솔 직접 접근자만 이용할 수 있도록 격리했습니다. 이를 통해 최고 관리자 권한 남용이나 원격 해킹 위험을 하드웨어 레벨에서 원천 차단하는 다층 방어(Defense-in-Depth) 보안 체계를 구축했습니다.

## 개발 후기

유기적인 업무 흐름의 실시간 시각화와 DFS 기반 의존성 제어, 그리고 물리적 에어갭 및 양자내성 암호 프레임워크까지 결합하여 보안이 중요한 요소로 작용하는 프로젝트에도 적용 가능한 관제/공유 플랫폼을 완성해 본 의미 있는 개발 경험이었습니다.
